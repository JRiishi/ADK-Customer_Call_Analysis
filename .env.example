# AWS Bedrock Configuration (Required for LLM agents)
# Set this as an environment variable: export AWS_BEARER_TOKEN_BEDROCK="your-token"
# Or use 'aws configure' to set up standard AWS credentials
AWS_BEARER_TOKEN_BEDROCK=your-bedrock-bearer-token-here

# Google API Configuration (Optional - only if using Gemini)
GOOGLE_GENAI_USE_VERTEXAI=0
GOOGLE_API_KEY=your-google-api-key-here
